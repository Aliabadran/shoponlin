@extends('home.layout.head')
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="{{ asset('css/custom.css') }}" rel="stylesheet">


@section('title')Cart @endsection
<body>
@include('home.layout.header')
<section class="inner_page_head">
   <div class="container_fuild">
      <div class="row">
         <div class="col-md-12">
            <div class="full">
               <h3>Your Shopping Cart<</h3>
            </div>
         </div>
      </div>
   </div>
</section>

@if ($errors->any())
<ul class="alert alert-warning">
@foreach ($errors->all() as $error)
<li>{{$error}}</li>
@endforeach
</ul>
@endif

@if (session('status'))
<div class="alert alert-success">{{ session('status') }}</div>
@endif


<div class="container">
  <br>
  <br>

@if ($cartItems->isEmpty())
  <div class="text-center">
      <p>Your cart is empty.</p>
      <a href="{{ route('admin.home') }}" class="btn btn-primary">Shop Now</a>
 </div>

@else

<div class="container">
    <form action="{{ route('cart.update', $cartItems->product->id )}}" method="POST">
        @csrf
        @method('PUT')
<div class="row">
    <div class="col-lg-8">
        <table class="table table-striped table-hover cart-table">
            <thead class="thead-dark">
                <tr>
                    <th scope="col"> id</th>
                    <th scope="col"> Product Image</th>
                    <th scope="col">Product</th>
                    <th scope="col">Quantity</th>
                    <th scope="col">Price</th>
                    <th scope="col">Total</th>
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach ($cartItems as $item)
                    <tr>
                        <td class="align-middle">
                            <div class="cart-id">
                                <span>{{ $item->id }}</span>
                            </div>
                            <td class="align-middle">
                                <div class="cart-product-image">

                                        <img src="{{ asset('storage/' . $item->product->photo) }}" alt="Photo" width="100">

                                </div>
                        <td class="align-middle">
                            <div class="cart-product-name">
                                 <span>{{ $item->product->name }}</span>
                            </div>
                        </td>
                        <td class="align-middle">
                            <!-- Update Quantity Form -->
                            <form action="{{ route('cart.update', $item['product']->id) }}" method="POST">
                                @csrf
                                @method('PUT')
                                <input type="number" name="quantity" value="{{ $item['quantity'] }}" min="1" value="{{ $item->quantity }}" class="form-control" style="width: 70px;">
                                <button type="submit" class="btn btn-sm btn-primary mt-1">Update</button>
                            </form>
                        </td>
                        <td class="align-middle">${{ number_format($item->product->price, 2) }}</td>
                        <td class="align-middle">${{ number_format($item->price, 2) }}</td>
                        <td class="align-middle">

                            <!-- Remove Item from Cart -->
                            <form action="{{ route('cart.remove', $item->id) }}" method="POST" style="display:inline;">
                                @csrf
                                @method('DELETE')
                                <button type="submit" class="btn btn-danger btn-sm">Remove</button>
                            </form>
                        </td>
                    </tr>
                @endforeach
            </tbody>
        </table>
    </div>


    <!-- Cart Summary -->
    <div class="col-lg-4">
        <div class="cart-summary p-3 bg-light rounded">
            <h4 class="text-center">Cart Summary</h4>
            <hr>
            <div class="d-flex justify-content-between">
                <span>Total Items</span>
                <span>{{ count($cartItems) }}</span>
            </div>
            <div class="d-flex justify-content-between">
                <span>Total Price</span>
                <span>${{ number_format($totalPrice, 2) }}</span>
            </div>
            <hr>
            <a href="" class="btn btn-success btn-block">Proceed to Checkout</a>
        </div>
    </div>
</div>

@endif
<br>
<br>
</div>
</div>

@include('home.layout.footerPage')




<script>
    document.querySelectorAll('.update-quantity').forEach(input => {
        input.addEventListener('change', function() {
            this.closest('form').submit();
        });
    });
</script>


